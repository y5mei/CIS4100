az vm create --resource-group vms --location canadacentral --name cli-1 --image UbuntuLTS --admin-username azuresure --generate-ssh-keys --verbose

az vm create --resource-group images --location canadacentral --name winServer01 --image Win2019Datacenter

// all the tags for Azures:
https://docs.microsoft.com/en-US/cli/azure/vm?view=azure-cli-latest#az_vm_create

// all the command reference:

https://docs.microsoft.com/en-us/azure/virtual-machines/linux/cli-manage


// create a resource group:
az group create --name images --location canadacentral

// create a linux does not need a admin-password
az vm create --name linuxServer01 --resource-group images --image debian --admin-username pig --generate-ssh-keys

// create a windows server need a admin-password
az vm create --name winServer01 --resource-group images --image Win2012R2Datacenter --admin-username pig --generate-ssh-keys --admin-password Myw1234567890123

// list all the vms
https://docs.microsoft.com/en-us/cli/azure/format-output-azure-cli
 az vm list --output table
 // list all the vms with details of running status
 az vm list -d -o table

// delete a Azure VM: use the --yes to make sure no user input
az vm delete --resource-group IMAGES --name winServer01 --yes

# open a port
az vm open-port --resource-group myResourceGroup --name myVM --port 80